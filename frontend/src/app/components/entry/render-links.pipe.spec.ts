import { RenderLinksPipe } from './render-links.pipe';

describe('RenderLinksPipe', () => {
  it('create an instance', () => {
    const pipe = new RenderLinksPipe();
    expect(pipe).toBeTruthy();
  });
});
